{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "sb": {
      "dependsOn": ["build"]
    },
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["lint"]
    }
  }
}
